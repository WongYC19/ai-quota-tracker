[project]
name = "ai-quota-tracker"
version = "1.0.0"
description = "Multi-provider AI quota and telemetry tracker — unified dashboard for Antigravity and future IDE providers"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "psutil>=5.9.8",
    "requests>=2.32.3",
    "urllib3>=2.3.0",
    "python-dateutil>=2.9.0",
    "pytz>=2024.2",
]

[project.urls]
Homepage = "https://github.com/WongYC19/ai-quota-tracker"
Repository = "https://github.com/WongYC19/ai-quota-tracker"

[project.scripts]
ai-quota-tracker = "src.orchestrator:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.uv]

[dependency-groups]
dev = [
    "pytest>=8.4.0",
    "ruff>=0.11.0",
    "black>=25.1.0",
    "pyinstaller>=6.14.0",
]

[tool.black]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py313"
